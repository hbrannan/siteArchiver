const app = (state={isDisplayingSite: false, currentSite: '<html><body>Hello, <b>world</b>.</body></html>'}, action) => {
  switch ( action.type ) {
    default :
      return state;
  }
}

export default app
